# دليل النشر الشامل على Vercel

## نظرة عامة

هذا المشروع عبارة عن تطبيق Full-Stack يتضمن:
- **Frontend:** React 19 + TypeScript + Tailwind CSS 4
- **Backend:** Express + tRPC 11
- **Database:** MySQL/TiDB
- **Authentication:** Manus OAuth

---

## المتطلبات الأساسية

1. **حساب Vercel:** إنشاء حساب مجاني على [vercel.com](https://vercel.com)
2. **قاعدة بيانات MySQL:** ستحتاج إلى قاعدة بيانات MySQL خارجية (موصى به: PlanetScale أو Railway)
3. **Git Repository:** رفع المشروع على GitHub/GitLab/Bitbucket

---

## الخطوة 1: إعداد قاعدة البيانات

### الخيار الأول: PlanetScale (موصى به)

1. اذهب إلى [planetscale.com](https://planetscale.com) وأنشئ حساب مجاني
2. أنشئ قاعدة بيانات جديدة
3. احصل على رابط الاتصال (Connection String)
4. قم بتشغيل الأوامر التالية لإنشاء الجداول:

```bash
# تثبيت Drizzle Kit
npm install -g drizzle-kit

# تطبيق Schema على قاعدة البيانات
DATABASE_URL="your_connection_string" pnpm db:push
```

### الخيار الثاني: Railway

1. اذهب إلى [railway.app](https://railway.app)
2. أنشئ مشروع جديد واختر MySQL
3. احصل على رابط الاتصال من لوحة التحكم
4. نفذ نفس الأوامر أعلاه

### الخيار الثالث: استخدام قاعدة بيانات Manus المدمجة

إذا كنت تستخدم Manus للاستضافة، قاعدة البيانات موجودة بالفعل ولا تحتاج لإعداد إضافي.

---

## الخطوة 2: رفع المشروع على Git

```bash
# إذا لم تكن قد أنشأت repository بعد
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/your-username/your-repo.git
git push -u origin main
```

---

## الخطوة 3: النشر على Vercel

### الطريقة الأولى: من خلال واجهة Vercel (موصى به)

1. اذهب إلى [vercel.com/new](https://vercel.com/new)
2. اختر Git Repository الخاص بك
3. اضغط على "Import"
4. في قسم "Environment Variables"، أضف المتغيرات التالية:

```env
DATABASE_URL=your_mysql_connection_string
JWT_SECRET=your_random_secret_key_here
NODE_ENV=production
```

5. اضغط على "Deploy"

### الطريقة الثانية: من خلال Vercel CLI

```bash
# تثبيت Vercel CLI
npm i -g vercel

# تسجيل الدخول
vercel login

# النشر
vercel

# اتبع التعليمات وأضف المتغيرات البيئية عند الطلب
```

---

## الخطوة 4: إضافة المتغيرات البيئية

في لوحة تحكم Vercel، اذهب إلى:
**Settings → Environment Variables**

أضف المتغيرات التالية:

| المتغير | القيمة | الوصف |
|---------|--------|-------|
| `DATABASE_URL` | `mysql://user:pass@host:port/db` | رابط الاتصال بقاعدة البيانات |
| `JWT_SECRET` | `your-secret-key-min-32-chars` | مفتاح سري للتشفير (32 حرف على الأقل) |
| `NODE_ENV` | `production` | بيئة التشغيل |

**ملاحظة مهمة:** إذا كنت تريد استخدام نظام المصادقة (تسجيل الدخول)، ستحتاج إلى:
- `OAUTH_SERVER_URL`
- `VITE_OAUTH_PORTAL_URL`
- `VITE_APP_ID`

هذه المتغيرات متوفرة فقط في بيئة Manus. إذا كنت تنشر على Vercel، يمكنك تعطيل المصادقة أو استخدام نظام مصادقة بديل.

---

## الخطوة 5: إعداد البيانات الأولية

بعد نشر المشروع، قم بتشغيل سكريبت البيانات الأولية:

```bash
# اتصل بقاعدة البيانات وقم بتشغيل
DATABASE_URL="your_connection_string" node seed-data.mjs
```

أو يمكنك إضافة البيانات يدوياً من خلال لوحة التحكم الإدارية.

---

## الخطوة 6: إعداد النطاق المخصص (اختياري)

1. في لوحة تحكم Vercel، اذهب إلى **Settings → Domains**
2. أضف نطاقك المخصص
3. اتبع التعليمات لتحديث DNS

---

## هيكل المشروع

```
mati_transport/
├── client/                 # الواجهة الأمامية (React)
│   ├── src/
│   │   ├── pages/         # الصفحات
│   │   ├── components/    # المكونات
│   │   └── lib/           # المكتبات
│   └── public/            # الملفات الثابتة
├── server/                # الواجهة الخلفية (Express + tRPC)
│   ├── _core/            # النواة الأساسية
│   ├── routers.ts        # إجراءات tRPC
│   └── db.ts             # وظائف قاعدة البيانات
├── drizzle/              # Schema قاعدة البيانات
│   └── schema.ts
├── vercel.json           # إعدادات Vercel
└── package.json
```

---

## الميزات المتوفرة

✅ **عرض أسطول السيارات** (30 سيارة متنوعة)
✅ **نظام طلب الشحنات** مع تسعير ذكي تلقائي
✅ **نظام طلب الإيجار السنوي** مع خيار السائق
✅ **تكامل واتساب** (زر عائم في جميع الصفحات)
✅ **لوحة تحكم إدارية** لإدارة الطلبات والسيارات
✅ **تتبع الطلبات** للمستخدمين المسجلين
✅ **تصميم متجاوب** يعمل على جميع الأجهزة
✅ **دعم كامل للغة العربية** مع RTL

---

## استكشاف الأخطاء

### المشكلة: خطأ في الاتصال بقاعدة البيانات

**الحل:**
- تأكد من صحة `DATABASE_URL`
- تأكد من أن قاعدة البيانات تقبل اتصالات من Vercel
- في PlanetScale، فعّل "Allow all IPs" مؤقتاً للاختبار

### المشكلة: الصفحات لا تعمل

**الحل:**
- تحقق من أن ملف `vercel.json` موجود في الجذر
- تأكد من أن Build Command هو `pnpm build`
- تأكد من أن Output Directory هو `dist`

### المشكلة: API لا يستجيب

**الحل:**
- تحقق من أن المسارات في `vercel.json` صحيحة
- تأكد من أن جميع المتغيرات البيئية مضافة
- راجع Logs في لوحة تحكم Vercel

---

## الأوامر المفيدة

```bash
# تطوير محلي
pnpm dev

# بناء المشروع
pnpm build

# تشغيل الإنتاج محلياً
pnpm start

# تطبيق تغييرات قاعدة البيانات
pnpm db:push

# تشغيل الاختبارات
pnpm test
```

---

## ملاحظات مهمة

1. **رقم الواتساب:** الرقم المدمج في الموقع هو `0596466303`
2. **لوحة التحكم:** يمكن الوصول إليها من `/admin` (تتطلب صلاحيات مدير)
3. **قاعدة البيانات:** تأكد من عمل نسخ احتياطية دورية
4. **الأمان:** لا تشارك `JWT_SECRET` أو `DATABASE_URL` مع أحد

---

## الدعم والمساعدة

- **Vercel Docs:** [vercel.com/docs](https://vercel.com/docs)
- **PlanetScale Docs:** [planetscale.com/docs](https://planetscale.com/docs)
- **tRPC Docs:** [trpc.io](https://trpc.io)

---

## البدائل لـ Vercel

إذا واجهت مشاكل مع Vercel، يمكنك استخدام:
- **Railway:** [railway.app](https://railway.app)
- **Render:** [render.com](https://render.com)
- **Manus (موصى به):** استخدام زر Publish المدمج

---

تم إنشاء هذا الدليل لمساعدتك في نشر موقع مؤسسة محمد الحسني للنقليات بنجاح. 🚚✨
